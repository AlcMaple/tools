修复在线观看时凭空出现的声音；优化稀饭搜索与取集数速度

> [!Tip]
> 已装旧版的用户：应用会自动检测并提示，点「重启安装」即可。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 优化

**在线观看**

- 稀饭搜索、取集数列表明显变快

---

### 修复

**在线观看**

- 修复点开在线观看时凭空出现声音：画面还停在「解析播放地址中…」就已经在响，暂停按钮按不停，退出播放页也还在响（Windows 上尤其明显）
- 修复明明能正常播放、却弹出「稀饭安全检查未完成」的误报
- 失败提示会说明当时卡在哪一步，不再一律归咎于「安全检查未完成」

---

### 已知限制

- 稀饭若日后把安全检查改为依赖第三方脚本，可能出现「一直转圈直到超时」，届时需调整过滤规则
- 未做代码签名 / 公证（macOS 首次打开需手动放行；macOS 不支持应用内静默更新）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.15.1_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.15.1_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.15.1_macos_arm64.zip` |
