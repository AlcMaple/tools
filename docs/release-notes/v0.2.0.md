围绕「追番」打造的一整套工作流上线，设置页重设计、文件浏览器增强。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 新增

**追番系统**

- 新增「我的追番」汇总页，统一管理观看状态、当前集数、绑定的观看链接
- BGM 详情页支持一键追番
- 我的追番列表行尾可一键在外部浏览器打开对应源的播放页观看
- 总集数支持手动填写，BGM 数据缺失（长寿番、特别篇、季番初期）也能继续跟进度
- 总集数留空时显示「连载中」
- 新增极简追番报告发到 QQ 邮箱，方便在手机上扫读

**番剧周历**

- 新增本季番剧周历页，按周一至周日列展示，当日列自动高亮
- 周历可自动发送长截图到 QQ 邮箱（需在设置里开启邮件功能）

**云同步**

- 数据分开同步，互不影响
- 同步加冲突检测与确认弹窗，明确显示本地 / 远端差异避免误覆盖
- 首次升级会自动从老数据迁移追番列表

**设置页**

- 重新设计为左栏分类导航、自动保存
- 下载路径会显示当前实际生效的位置，留空时显示系统默认下载文件夹
- 一键打开当前下载路径

**文件浏览器**

- Windows 删除到回收站新增「分片回收」
- 删除操作新增进度加载层，多文件批量删除可见进度
- 占用中的文件自动处理后再删
- 右键菜单自动翻转

---

### 优化

- 嗷呜源下载更稳定，复制直链新增缓存
- 优化 BGM 搜索稳定性
- BGM 搜索支持别名匹配

---

### 修复

**文件管理**

- 修复本地媒体库文件按数字排序错误

**追番 / 番剧**

- 修复嗷呜源在站点改版后无法使用

**云同步**

- 修复启动时偶发把"未改动"误标为本地有变更

**其他**

- 修复跨时区凌晨日期回退一天
- 修复已知的问题

---

### 已知限制

- 未做代码签名 / 公证（macOS 首次打开需手动放行）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.2.0_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.2.0_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.2.0_macos_arm64.zip` |
