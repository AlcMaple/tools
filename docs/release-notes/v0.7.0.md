我的追番新增小说进度与推荐人筛选，修复 BGM 限流与更新检查问题

> [!Tip]
> 已装 v0.6.0 的用户：应用会自动检测并提示，点「重启安装」即可。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 新增

**我的追番**

- 小说类追番改用「卷 / 章」记录阅读进度，支持 SS2、短篇、后记等自定义文本
- 推荐列表支持按推荐人筛选

---

### 优化

- 列表筛选器优化交互

---

### 修复

- 修复 BGM 搜索 / 详情被限流时没有提示、也没有等待倒计时的问题
- 修复开启自动检查更新后，已是最新版时设置页一直卡在「检查中…」

---

### 已知限制

- 未做代码签名 / 公证（macOS 首次打开需手动放行；macOS 不支持应用内静默更新）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.7.0_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.7.0_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.7.0_macos_arm64.zip` |
