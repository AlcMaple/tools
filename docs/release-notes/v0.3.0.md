应用内自动更新上线 —— 之后再发版本无需手动下载安装包。

> [!Important]
> **本次需手动下载**：当前 v0.2.0 用户里没有自动更新代码，需要从下方下载 v0.3.0
> 手动安装一次。**装上 v0.3.0 后**，后续 v0.3.1 / v0.4.0 / ... 会自动检测并下载,
> 你只需要点一下「重启安装」。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 新增

**应用内自动更新**

- 启动后静默检查 GitHub 是否有新版本
- 设置页「关于」新增「检查更新」按钮和「自动检查更新」开关

---

### 已知限制

- 未做代码签名 / 公证（macOS 首次打开需手动放行；macOS 不支持应用内静默更新）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.3.0_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.3.0_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.3.0_macos_arm64.zip` |
