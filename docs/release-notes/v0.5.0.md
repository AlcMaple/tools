自动更新新增国内加速

> [!Tip]
> 已装 v0.4.0 的用户：应用会自动检测并提示，点「重启安装」即可。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 新增

**追番**

- 「我的追番」支持手动添加番剧，封面本地缓存，跨设备同步
- 本地搜索可按 BGM 别名命中

**其他**

- 自动更新支持国内加速
- BGM 搜索栏新增历史记录
- 「好看集」支持给单集写备注
- 周历每部番的播放源改成单按钮 + 弹窗挑选

---

### 优化

- 封面加载更稳更快
- 启动不再白屏闪烁
- 切换页面、滚动长列表不再卡顿
- 优化源站打不开的提示

---

### 修复

**搜索 / BGM**

- 修复系统代理环境下连不上 BGM / 萌娘的问题
- 修复搜索动漫时偶发报错、切换搜索类目时结果被清空
- 修复部分番剧简介获取异常

**界面**

- 修复筛选数量变化时整行布局被挤动
- 修复「好看集」备注气泡定位与残留

---

### 已知限制

- 未做代码签名 / 公证（macOS 首次打开需手动放行；macOS 不支持应用内静默更新）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.5.0_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.5.0_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.5.0_macos_arm64.zip` |
