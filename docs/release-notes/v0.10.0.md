新增 macOS 菜单栏托盘，修复一些已知问题

> [!Tip]
> 已装 v0.9.0 的用户：应用会自动检测并提示，点「重启安装」即可。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 新增

**macOS**

- 新增顶部菜单栏托盘图标

---

### 优化

- 移除了顶部重复的搜索框

---

### 修复

- 修复打包版主窗口打不开的问题
- 关闭「关闭到托盘」后，点窗口 X 直接退出程序
- 从托盘恢复窗口后 Dock 图标显示不正常

**界面**

- 右键全选改为只选中所在文本块

---

### 已知限制

- 未做代码签名 / 公证（macOS 首次打开需手动放行；macOS 不支持应用内静默更新）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.10.0_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.10.0_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.10.0_macos_arm64.zip` |
