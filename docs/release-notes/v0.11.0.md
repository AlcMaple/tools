添加响应式设计，新增 Bangumi 账号登录，修复稀饭下载相关问题

> [!Tip]
> 已装 v0.10.0 的用户：应用会自动检测并提示，点「重启安装」即可。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 新增

**界面**

- 新增响应式布局

**Bangumi**

- 新增 Bangumi 账号登录（内嵌登录窗自动填充），登录后元数据搜索更快、更少触发限流

**设置**

- 新增「控制台」开关，便于排查问题

---

### 优化

- 推荐功能支持填写推荐理由
- 新增下载队列下载失败时切换线路重新下载
- 动漫查询只有一个结果时仍以列表展示
- 网络受限 / 被拦截时的错误提示
- 界面文案与图标细节优化

---

### 修复

**下载**

- 修复稀饭（Xifan）部分线路下载到无法播放 / 损坏的文件，却显示「完成」的问题
- 修复稀饭搜索偶尔返回错误响应、搜不出结果的问题

**界面**

- 修复大屏下动漫信息页返回按钮不显示的问题

---

### 已知限制

- 未做代码签名 / 公证（macOS 首次打开需手动放行；macOS 不支持应用内静默更新）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.11.0_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.11.0_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.11.0_macos_arm64.zip` |
