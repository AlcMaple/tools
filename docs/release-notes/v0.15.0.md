在线播放器换成自研控制条，进度条悬停可看时间点；修复暂停后仍有声音

> [!Tip]
> 已装旧版的用户：应用会自动检测并提示，点「重启安装」即可。

> [!Note]
> 本应用依赖系统 ffmpeg，请确保已安装并加入 PATH。
> Windows 从 [ffmpeg.org](https://ffmpeg.org/download.html) 下载；macOS 推荐 `brew install ffmpeg`。

> [!Tip]
> macOS 首次打开提示"无法验证开发者"时，请右键 App → 选择"打开"，
> 或执行 `xattr -d com.apple.quarantine /Applications/MapleTools.app`。

> [!Warning]
> 当前为开发中版本（v0.x），界面与数据格式可能在后续版本调整。

---

### 新增

**在线观看**

- 播放器换成自研控制条：鼠标悬停在进度条上可以看到对应的时间点，另有音量条和 0.5x–2x 倍速

---

### 优化

**在线观看**

- 点开一集到出画面明显变快
- 缓冲时播放区中央会显示「缓冲中…」，不再是一片纯黑看不出在不在动

---

### 修复

**在线观看**

- 修复暂停后仍能听到声音、恢复播放变成两路声音叠加、退出播放页声音还在响
- 修复退出播放页后临时文件残留，要等下次启动才清掉

---

### 已知限制

- 未做代码签名 / 公证（macOS 首次打开需手动放行；macOS 不支持应用内静默更新）
- 仅发布 Windows x64 与 macOS arm64

---

### 下载

| 平台 | 文件 |
|---|---|
| Windows 10/11 x64 | `MapleTools_0.15.0_windows_x64.exe` |
| macOS 11+ arm64 | `MapleTools_0.15.0_macos_arm64.dmg` |
| macOS 11+ arm64（便携版） | `MapleTools_0.15.0_macos_arm64.zip` |
