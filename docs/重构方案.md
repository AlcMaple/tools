# 文档与规范重构方案（2026-06-29）

> 本文档是一次性重构的执行记录 + checklist。涵盖三件事:① AGENTS.md 6–9 节对照最新代码的修订;② 三处代码小瑕疵处理;③ docs/ 目录整理(英文文件夹分类 + 合并/删除)。
> 执行完成后本文件可移入 `docs/archive/` 或删除。

## 背景

AGENTS.md 长期没维护,而最近若干提交(BGM 登录鉴权 `1329c37`/`f524a73`、搜索带登录 cookie 提速 + CF 报错分类 `6277487`、HttpSession 改 netRequest `91c9231`)让第 6–9 节滞后。经 3 个子 agent 逐条对照最新代码核验,**结论:所有红线代码均守住,无代码走偏,问题主要是「文档落后于代码的正当演进」**,外加 3 处非红线的轻微代码瑕疵。

---

## 一、AGENTS.md 6–9 节修订(全部采纳)

### 第 5 节(目录职责)
- [ ] `shared/` 职责列表补 `http-session.ts`(91c9231 后升为 girigiri+xifan 共享件)、`scrape-guard.ts`

### 第 6 节(网络与抓取)
- [ ] **6.1** 补「下载/二进制流例外」边界:下载器(`mp4-range-downloader`、HLS 分片)、aowu `secure` 协议、WebDAV 走 Node http/https/fetch 是既有例外,红线只约束 HTML/API 抓取
- [ ] **6.2** 新增「鉴权」小节:token→`api.bgm.tv` 作 `Authorization: Bearer`;cookie→`bgm.tv` HTML 作 `Cookie` 头(登录态搜索 ~0.6s vs 匿名 ~16s);二者都存 `userData/bgm_auth.json`,不回传 renderer 明文;登录校验 `verifyBgmLogin` 只在 `bgm:verify-login` 显式触发、无轮询
- [ ] **6.3** 补 L3 熔断器(`bgm/api-circuit.ts`)说明:429 后阶梯冷却、半开仅靠下一个自然请求试探、**绝不主动探测**,属合规防御
- [ ] **6.4** 补 `RateLimiter` 的滚动窗口配额(`maxPerWindow`/`windowMs`)+ `softThrottle` 软恢复
- [ ] **6.x(CF)** 补 Cloudflare 报错分类原则(与第 9 节呼应)

### 第 7 节(IPC)
- [ ] surface 清单补 3 个文档没提的:`miaoyuApi`、`updaterApi`、`screenshotApi`
- [ ] (`library-updated` 改名为合规 channel 后,无需再作反例,见「二」)

### 第 8 节(状态管理)
- [ ] 补:瞬时 UI store(`uiStore`,抽屉开合)可不落 localStorage、不需要 normalize,是合规例外
- [ ] 补:**页面级 localStorage 状态(带 normalize、不存机器路径)是合法的第二模式**(妙语库现状,见「二」判断题 2)
- [ ] store 示例清单补 `recommendationStore`、`uiStore`

### 第 9 节(错误处理)
- [ ] 笔误修正:`RateLimitError(retryAfterSec, msg)` → 实际构造参数是 `waitSeconds`(`retryAfterSec` 是渲染层 FriendlyError 的字段,勿混)
- [ ] 补 CF 分类原则:严格匹配 `cf-mitigated=challenge/block/managed`、`Just a moment`、`cf-chl`,**不用裸 `cloudflare` 关键词**(BGM 诊断串恒带 `server=cloudflare`,会误判)
- [ ] 补:登录/鉴权失败走状态位 `getBgmAuthStatus` + `BgmLoginChip`,**不进 friendlyError**(是状态不是错误)
- [ ] 第 9 节不逐条列全 23 类(会再过时),改为「完整分类清单以 `errorMessage.ts` 为准」+ 点名 CF / 限流 / 鉴权三个最易误改的关键原则

---

## 二、三处代码小瑕疵(判断题处理结果)

1. **`library-updated` channel 命名不规范** → ✅ **改代码**重命名为 `library:updated`。
   - `src/main/index.ts:239,267`(send)、`src/preload/index.ts:266,267`(removeAllListeners/on)四处同改。内部 channel、两端同改、无持久化,低风险。
2. **妙语库持久化状态写在 `pages/MiaoyuLibrary.tsx` 内,未抽 store** → ✅ **不重构**,在 AGENTS §8 承认「页面级 localStorage 状态」为合法第二模式。
   - 理由:功能在用、已遵守 normalize + 不存机器路径精神,抽 `miaoyuStore` 风险大于收益。需要时另开任务。
3. **CF 诊断码自标「临时诊断…可删」(`bgm/search.ts` `diagnoseFailure`)** → ✅ CF **分类**(`errorMessage.ts`)是永久的,写进规范;`search.ts` 里的临时指纹日志**先保留不删**(可能仍在帮助定位线上 CF 行为),待确认后再清理。

---

## 三、docs/ 目录整理(英文文件夹 · 先方案后执行)

### 文件夹归类(新建 5 个英文文件夹;已有 `design-mockups/ ideas/ release-notes/ screenshots/ Windows删除回收站/` 不动)

| 目标文件夹 | 收纳文件 |
|---|---|
| `docs/scraping/` | bgm-集成参考手册 · aowu-FantasyKon-逆向与反爬手册 · 资源站爬虫方式与新站接入参考 · 浏览器方案 · bgm-国内直连替代方案调研 · 替代BGM-需要的信息清单 |
| `docs/regression/` | bgm-简介解析-回归用例 · xifan-下载链接-集数补零-回归用例 |
| `docs/troubleshooting/` | girigiri动漫列表和下载 debug · dev-ctrl-c-优雅退出 |
| `docs/release/` | how-to-release · release · 自动更新-国内加速 · 自动更新国内加速-Windows验证指南 |
| `docs/design/` | GUI_设计规划 · LIGHTS_DESIGN · 前端应用开发流程 · 在线播放 |
| `docs/archive/` | XIFAN_TEST(测早废的 Python 原型) · anime-tracking-plan(功能已落地的接力棒) |
| 留 docs/ 根 | 功能索引.md(文档总入口) · Git.md(提交规范速查) |

### 合并/删除评估结论
- `XIFAN_TEST.md` → archive(测的是 `archive/python_legacy/` 旧 Python 原型,早废)
- `anime-tracking-plan.md` → archive(追番/周历/跳转已全部落地)
- `在线播放.md` → design(在-app 播放 HLS 缓存早期目标,功能未做,留作设计目标)
- `浏览器方案.md` → scraping,但「当前方案 https/axios+cheerio」一行与现 net 红线冲突,需标注「axios 已废,现走 Electron net」
- `release.md` vs `how-to-release.md` → 都进 release/,**暂不合并**(前者通用 Release 教育+notes 模板,后者本项目流程)
- `Git.md` → 留根(与 §11.1 重叠但 type 表更全,当速查)

### 移动会改断的 inbound 链接(执行时连带改)

| 移动的文档 | 引用处 |
|---|---|
| bgm-集成参考手册 → scraping/ | CLAUDE.md:5,57 · AGENTS.md:96 · `animeTrackStore.ts:504` · `AnimeInfo.tsx:1500` · `net-request.ts:5` |
| xifan-集数补零-回归用例 → regression/ | `siteApi.ts:88,95` · `xifan/download.ts:91` |
| bgm-简介解析-回归用例 → regression/ | `功能索引.md:77,134,154` |
| 自动更新-国内加速 → release/ | CLAUDE.md:118 · AGENTS.md:277 |
| aowu-逆向手册 → scraping/ | CLAUDE.md:46 |
| how-to-release → release/ | CLAUDE.md:118 |

---

## 四、README + CLAUDE 顺带修

- [ ] README 目录结构加一行 `archive/`(历史原型,不参与构建);`scripts/` 已在,不动
- [ ] CLAUDE.md:11 `python/` → `archive/python_legacy/`(目录已搬)
- [ ] CLAUDE.md:45 girigiri 行去掉 `http-session.ts`(已移到 `shared/`);shared 行补 `http-session.ts`
