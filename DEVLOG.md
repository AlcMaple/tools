# 开发日志（DEVLOG）

> 这里是全部开发日志的稳定入口，只保留写入规则、专题索引和当月未归类记录，避免每次任务都加载无关历史

## 写入规则

1. 每次提交对应一条日志，并在提交前先写日志、确定最终提交标题；记录标题固定为 `## YYYY-MM-DD <commit subject>`，其中 `<commit subject>` 必须与 Git 提交标题逐字一致
2. 任务按功能、模块单独提交。可归入专题的记录写入 `docs/devlog/<年份>-<专题>.md`；同专题同一年共用一个文件，新记录在上，跨年新建文件
3. 纯文档、杂项等暂时无法归类的单次改动写在本文件。开始新月份时，将上个月的未归类记录移到 `docs/devlog/archive/YYYY-MM.md`；若后来形成明确专题，再整体迁入专题文件
4. 日志用于快速理解一次提交的最终结果：说明修改后的效果，并按复杂度展示关键代码决策、状态变化和底层数据流；不要逐文件复述 diff
5. 简单改动写清效果即可；复杂功能优先配数据流 / 架构图，图片放 `docs/devlog-assets/` 并在对应日志中引用。
6. 日志保留理解实现所需的关键决策；较长的失败尝试、方案权衡和阶段复盘放 `docs/ideas/`，由对应日志链接。

## 2026 专题日志

- [安装包](docs/devlog/2026-安装包.md)
- [网页版](docs/devlog/2026-网页版.md)
- [妙语库](docs/devlog/2026-妙语库.md)
- [在线观看](docs/devlog/2026-在线观看.md)
- [BGM 登录功能](docs/devlog/2026-BGM登录功能.md)
- [稀饭账号](docs/devlog/2026-稀饭账号.md)
- [动漫查询](docs/devlog/2026-动漫查询.md)

## 当前未归类记录（2026-08）

## 2026-08-16 fix(app): 修复退出时因下载速度广播导致孤儿进程空转

**效果**：

1. 退出时不再出现「终端已关、Electron 还在后台 97.5% CPU 空转」的孤儿进程

**根因与修法**：

```text
事故链：Ctrl+C → SIGINT → app 开始退出 → 渲染帧销毁
  → speed-tracker 每秒心跳还在向「帧已没了」的窗口 webContents.send
  → 抛 Render frame was disposed → 退出流程被绊住（该实例再无日志）
  → 进程活着但父链已死 → 孤儿 + 空转
```

1. `speed-tracker.ts`：发送前 `isDestroyed()` 跳过 + `try/catch` 兜「帧先走一步」的竞态窗口；新增 `stopSpeedBroadcast()`，`before-quit` 时停表——常驻心跳本身是有意设计（顶栏网速显示，空闲时每秒广播 0 的成本可忽略），缺陷只在「不设防」而非「不停」
2. `index.ts` dev-only 孤儿看护：每 5 秒查 `process.ppid === 1`（被过继给 launchd = 父链已死）就 `app.quit()`；`unref()` 不阻事件循环，开销为一次系统调用级。打包版不启用（Finder/Dock 启动父进程本来就是 launchd）

**边界 / 风险**：

1. 用户实例卡死时无任何日志产出，speed-tracker 是唯一抓到的退出期现行犯，不排除 WebDAV 同步收尾亦有参与——看护兜底保证即使再卡也不再烤机
2. 竞态窗口仅毫秒级宽（帧销毁瞬间撞上每秒 tick），4-24 引入以来首次踩中属概率事件

## 2026-08-15 fix(sync): 修复桌面端网页登录成功报未识别会话令牌

**效果**：

1. 桌面端登录网页版后不再错误提示“登录成功但没有收到会话令牌”
2. 登录成功后保存的 JWT 会在追番读取、上传等后续同步请求中以生产与本地开发环境都能识别的 Cookie 名回传，桌面端可继续使用既有云同步接口

**关键会话流**：

```text
桌面端 POST /api/auth/login
  → 生产：Set-Cookie __Host-mt_session=<JWT>
  → 开发：Set-Cookie mt_session=<JWT>
  → 桌面主进程识别两种名称并保存 JWT
  → 后续 /api/tracks/sync 请求同时携带两种 Cookie 名
  → 对应环境读取自己的会话名并校验原有 JWT
```

**边界 / 风险**：

1. 已安装的旧版桌面端没有该兼容逻辑，升级后需重新登录网页版账号；JWT 的过期和改密码后的既有失效机制不变。

## 已归档的单次记录

- [2026-07](docs/devlog/archive/2026-07.md)
