# AI 生成规范

## 网络请求

- ❌ 新增抓取逻辑用 Node `https.get`/`https.request`
  后果：Node `https` 不读系统代理，Clash 等 fake-ip 代理下域名解析成 `198.18.x` 假地址，直连黑洞超时

- ❌ 限流/5xx 失败后应用层自动重试、周期性探测「恢复了没」、或 catch 后静默 `return null`
  后果：惩罚窗口里加戳会加重限流；静默吞错让用户以为「没结果」而不停手动重试，等于放大伤害

- ❌ 播放器收到 `<video>` / HLS / DASH 错误后，不区分全局断网就刷新签名地址、标坏线路或自动切线
  后果：短暂断网会被误判成所有线路依次损坏，恢复网络时当前线路已经被淘汰，只剩错误页或 iframe。
  必须先确认全局连通性；断网只保存源 / 线路 / 集数 / 时间点，响应 `online` 事件同线路恢复一次，不周期探测

- ❌ 把首次起播的 `play` / `playing` 和播放中的 `waiting` / `seeking` 共用一个默认开启的缓冲闸门
  后果：媒体地址刚设置好时可以先显示「描线中」并保持暂停；用户点击原生播放控件后，首次 `playing` 却又被当成中途卡顿，
  纸片闪回、倍速/静音预热被误触发，用户会以为自动播放或线路又失败了。必须把首播和重缓冲分成两段状态：
  首次只设置 `src` / `load`、保持暂停；首次 `playing` 只标记“已经开始播放”并收起初始提示；只有之后真实的 `waiting` / `seeking`
  才能打开缓冲闸门。回退到跨域 `iframe` 时使用对方自己的控件和状态，不把主页面 `<video>` 的闸门逻辑套进去。
  每次改动都要用固定事件序列回归：`src/load → canplay → paused → user play → first playing（不显示缓冲）→ later waiting/seek（才显示缓冲）`。

- ❌ 传输层、缓存层、HLS/DASH 库和播放器各自独立重试同一媒体请求
  后果：一条 503 / 断流会按层数相乘成请求突发。重试预算必须只有一份：HTTP 4xx/5xx 不重试；
  仅明确的传输中断可有界、错峰续传；预取已经失败的分片不得再由直连路径重复取一遍

- ❌ 换集 / 换源时只清缓存 Map 或丢弃旧 Promise 结果，不中止真正的媒体请求和播放器实例
  后果：旧 HLS/Shaka/代理请求会在切换后继续占连接，甚至晚到后覆盖新源。每个请求必须有 owner、代次、
  deadline 和 abort；顺序是销毁播放器 → detach media → abort 缓存/预取/代理 → 再启动新源

- ❌ 带 BGM 登录 cookie 的请求配随机/不一致的 User-Agent
  后果：真实事故——BGM 把登录会话绑定在登录时的 UA 上，UA 一换整套 cookie 被当匿名，「登录提速」上线近一周形同虚设且引发「启动掉登录」死循环

- ❌ 上 Playwright 常驻浏览器去抓取
  后果：要的是浏览器的网络栈，不是它的 JS 渲染——BGM/萌娘都是服务端渲染 HTML、没有 JS 挑战，上真浏览器只会更慢、多吃几百 MB 内存，纯属浪费

## 错误处理

- ❌ 统一兜底错误文案（比如所有失败都显示"网络请求失败"）
  后果：真实事故——用户被限流时以为自己断网了，一直手动重试，反而把限流打得更狠

- ❌ 用裸 `cloudflare` 关键词判断是否被 CF 拦截
  后果：BGM 诊断串本来就恒带 `server=cloudflare`，裸关键词会把正常响应误判成拦截

## 调试日志

- ❌ 排查问题时把日志加在 `console.log`/`console.warn`，指望用户去翻网页 devtools
  后果：用户只看终端（`npm run dev` 那个进程的 stdout），网页控制台从不看——日志加了等于没加，
  还得再来一轮才发现「控制台没输出」说的是终端。网页里发生、只有浏览器知道的事件（`<video>` error、
  套娃回退 iframe、resolveSource 失败……），要用 `sendBeacon`/`fetch(keepalive)` 打回一个服务端
  日志接口，让服务端 `console.log` 落进它自己的终端；不要留在浏览器那一侧

## 数据持久化

- ❌ 把本机绝对路径（如 `archivist:///Users/xxx/.../cover.jpg`）写进要跨设备同步的数据
  后果：真事故——封面路径同步到另一台设备后路径不存在，封面全裂

- ❌ 一份数据拆成「DB 行 + 磁盘文件」两半后，只在部分写路径上维护一致性
  后果：真事故——本地上传封面（`cover` 列存哨兵路径 + `cover_mime`，字节在 `coversDir`）在单条
  PUT/DELETE 上清得好好的，整包同步那条却只写 `cover`：`cover_mime` 没清、文件没删，孤儿只增不减，
  读接口还认旧 `cover_mime` 继续吐早该失效的图。两半必须在**每一条**写路径（含批量删除）上同进同退，
  备份也要一起备、一起恢复。固定文件名上的异步 `unlink` 还必须与后续上传按条目串行，并在真正删除前
  复查 DB：否则旧封面的删除可能晚于新文件写入完成，反手删掉用户刚上传的图

## 边下边读（流式落盘同时被读）

- ❌ 用 `WriteStream.write()` 的**返回值时机**推进「已落盘字节数」，或写完最后一块立刻置「下完了」
  后果：真实事故——在线播放的 mp4 预抓缓存（`shared/media-cache.ts`）按 `write()` 返回后立刻 `written += len`，读取端据此去读那一段，结果数据还堵在 WriteStream 内部缓冲里：读到 0 字节，第一块时连文件都还没建出来（`ENOENT`，探测脚本必现）。同理 `done` 在 `end()` 之前置位，读取端会在最后几 KB 没落盘时就收到「下完了」，尾部静默截断

- ❌ 只看一次平均测速或平均码率，就认定「一条顺序流肯定够播放」
  后果：真实事故——稀饭线路一的 600MB / 23:42 样本平均消耗约 422KB/s，但单连接慢段只有 226~484KB/s；平均值看似接近，实际缓冲会不断被吃空，表现为播几秒停几秒，暂停一会儿又变顺

- ❌ 用 idle 超时无条件回收还有读取流挂着的会话
  后果：`<video>` 暂停时它那条响应流**不会**关闭，回收会把流 `error` 掉——用户暂停一分钟再点播放直接变成播放失败

- ❌ 以为网页 `<video>` 暂停后仍会持续预加载，靠 `pause()` 等够缓冲再恢复
  后果：真实 Chromium 在约 2.3 秒缓冲后停止拉流，等待逻辑永远达不到目标水位，播放器看起来一直卡在加载中

## UI / 样式

- ❌ 选中态/hover 态切换时改变盒模型尺寸（只在选中态加 border、加粗字重、改 padding）
  后果：相邻元素被挤一下，出现布局抖动，chip/tab/pill/列表项高发

- ❌ 计数 / 徽章 / 溢出提示（「×6」「99+」）**插进流里**跟按钮排排坐
  后果：数字一出现就把后面的按钮往右顶，用户正在连点的目标从指尖底下跑掉。同一族的还有
  「临时状态文字插进流里」那条——都要绝对定位浮在旁边，不要参与排版

- ❌ 绝对定位的浮层（气泡 / 提示 / 角标）只给 `left/right` 不给 `width: max-content`
  后果：没有宽度的绝对定位盒子按「可用空间」收缩，内容会从盒子里溢出去，指向性元素
  （气泡尾巴、箭头尖）跟着偏出目标——已踩：网页版观望气泡尾巴偏出「在追」按钮 31px

- ❌ 装饰性痕迹（下划线、圈注、贴纸）用「距卡片左上角 N px」这种魔数定位
  后果：卡片宽度 / 封面列宽 / 断点一变，痕迹就飘到别的元素上。让它长在被装饰的元素
  自己身上（`::after` / `::before`）

- ❌ 临时状态文字（「已保存」/ 报错 / 校验提示）**插进文档流里**才出现
  后果：跟上面那条同一个病，只是换成了「凭空插一行」——一保存，「已保存」把整行按钮往下顶（已踩：网页版设置页）。用户原话：「我希望这些类似 hover 的信息出来不要改变其他布局」

- ❌ 给用户写「自曝式」文案：解释自家实现、数据存哪、为什么这么设计、系统有什么局限
  后果：真实反馈——用户看到「你的数据只属于这个账号，和桌面版各自独立」「登录用，创建后不可修改，最长 12 个字符」「出于安全，问题和答案都不会显示出来」「来源 Bangumi · 14 天缓存」，原话是「好像暴露自己作品的弊端一样，什么都打开天窗说出来，大多数网站都没有的」。写这些是在跟**开发者自己**说话，不是跟用户；界面自明的东西再解释一遍只剩噪音

- ❌ 新加的可滚动区域（`overflow-*auto` 容器、`textarea` 等）用浏览器原生滚动条
  后果：原生滚动条又粗、又是系统配色，跟应用统一的 4px 细条不一致，一眼出戏（已踩：锦囊妙计记录备注）

- ❌ 用浏览器原生 `<select>`（及任何会弹出系统层的原生控件）的默认样式
  后果：展开后是系统弹层——蓝色高亮、系统字体、圆角/间距全不对，跟 MD3 token 毫无关系，暗色下尤其出戏（已踩：网页版密保问题下拉）。跟上面「原生滚动条」是同一类病，**原生控件的默认外观永远不会长得像我们的设计系统**，别指望调几个属性能救回来

- ❌ 下拉 / 浮层 / 菜单的宽度由内容撑开，跟触发它的那个容器不一致
  后果：浮层比触发器宽出或窄进一块，边缘对不齐，看着像两个不相干的东西叠在一起（已踩：网页版用户名 chip 的下拉菜单）

- ❌ Tailwind 透明度修饰符写非 5 倍数的值（`bg-primary/12`、`border-x/7`、`bg-y/6`）
  后果：**Tailwind 的 opacity 刻度只有 5 的倍数**（0/5/10/15…100），非倍数的类**根本不生成，且不报任何错**——元素静默掉回默认值。`border-b` 会掉回 preflight 的默认边框色 `#e5e7eb`，暗色下就是一道刺眼白线（已踩：网页版设置页的键值分隔线）。`bg-primary/12` 则是**底色直接不存在**——周历「今天」chip 的粉色底从上线起就没渲染过，因为边框 `/30` 和文字色是有效值，看着像对的，没人发现

- ❌ 想用 CSS 消掉 `<video>` 原生控件的焦点描边 / 焦点态
  后果：白写。原生控件活在 UA shadow DOM 里，实测（Electron 同款 Chromium，已踩：在线播放页黄框）——
  `::-webkit-media-controls-*`（播放 / 静音 / 全屏 / 进度条）作者样式表**能**选中（改 `opacity` 生效）；
  但 ⋮ 溢出菜单的伪元素是 `::-internal-media-controls-overflow-button`，`-internal-` 前缀**不匹配**，黄框够不着；
  而焦点落在静音键上时音量条会**展开**，那是布局变化不是描边，`outline` 再怎么写都治不了

- ❌ 拦 `<video>` 的焦点只监听 `focusin`
  后果：漏掉最常见的一条路径。焦点**已在** `<video>` 宿主上时（点过画面就是这个状态）再按 Tab 走进 shadow 控件，
  shadow DOM 把 target 重定向成宿主，而宿主自始至终没变 ⇒ `focusin` **不触发**，拦截逻辑根本不执行

- ❌ 为了绕开原生控件，直接把 Tab 键在整页 `preventDefault`
  后果：页内的输入框、弹窗表单全部没法用 Tab 跳转，用户明确要求「表单里的 tab 换行功能要有」

- ❌ 设计稿（mockup HTML）只放在 `scratchpad/` 或某台机器本地，不提交进仓库
  后果：真实事故——「我的追番」的设计稿写在 `scratchpad/tracks-mockup.html`（`TracksPage.tsx` 头注释还指着它），但 scratchpad 不进 git。换一台机器 / 换一个会话开发时，设计稿等于不存在，只能对着线上截图猜原设计，改样式没有基准。这个项目 **Mac 开发 + Windows 开发两头切**，尤其致命：一头写了稿，另一头看不到

## 服务端 / 部署（网页版）

- ❌ 把用户名、短链名等用户可注册标识直接挂在根路径（`/:username`），再把登录、验证码等接口也放在根路径
  后果：用户可注册与官方接口同名的标识，路由顺序或重写规则一变，接口就可能被用户页面抢占；只靠“当前静态路由写在前面”不是安全边界

- ❌ 起 HTTP 服务用框架默认的监听地址（`serve({ fetch, port })` 默认绑 `0.0.0.0`）
  后果：真实事故——线上 node 绑了 `0.0.0.0:3000` 且没防火墙，公网 `http://<ip>:3000` 直接通，**整个 nginx 白装**：HTTPS 被绕过（登录密码明文过公网）、HSTS/证书全不生效、`X-Forwarded-For` 随便伪造导致按 IP 限流一文不值。反代场景下「能从外面访问」不是功能，是洞

- ❌ 只给「显眼的那个」入口加限流
  后果：真实事故——`/forgot` 加了限流（因为密保答案熵低，想得到），`/login` 反而裸奔，连打 7 次错密码全是 401 从不 429。凡是「拿凭证来猜」的入口都是同一类，挑着加 = 漏

- ❌ 用户数据 / 密钥放在部署目录里
  后果：重新部署（`rm -rf` 再传）会连用户一起删；`AUTH_SECRET` 进仓库 = 公开

- ❌ 写完流式接口（SSE / chunked）只在本地 dev 验「逐字出现」就当它成了
  后果：真实事故——dev 直连 node 没有反代，线上 nginx `proxy_buffering` 默认 on，把整条流攒满再一次性下发，用户看到的是「等一会儿整段蹦出来」。流式响应要显式带 `X-Accel-Buffering: no`（放在应用里，别指望每台机的 nginx 配置）

- ❌ 只在 dev 验证浏览器端新用到的 URL scheme（`blob:` / `data:` / 新域名）
  后果：真实事故——dev 不发 CSP，线上 `img-src` 没有 `blob:`，Canvas 生成的分享图预览是裂图，而下载正常（`<a download>` 不受 `img-src` 管），症状特别像「图没生成出来」

- ❌ 改 CSP 只改一处
  后果：真实事故——CSP 有两份且**同时生效、取交集**：`server/security.ts` 的响应头 + `vite.config.ts` 注入 `index.html` 的 `<meta>`。只改响应头那一轮线上毫无变化，白排查一轮。两处必须一起改，且 `<meta>` 是构建期注入的，热传服务端 TS 不会更新它

- ❌ 用「字段 A 有值」当「这条记录已补全」的闸门
  后果：真实事故——回填函数写成「`bgm_tags` 非空就 return」，而在线搜 Bangumi 加进来的条目**自带标签、没有日期和评分**，于是详情请求一次都不发，那批卡永久停在缺日期缺评分。闸门要逐字段判「还缺什么」，不是挑一个字段代表全体

## 工程习惯

- ❌ 识别出「唯一的工程风险是 XXX」后，只留一句注释「未来出问题再对齐/再修」就交付
  后果：真实案例——已登录搜索只顶掉 UA 没动 sec-ch-ua，留注记「未来 BGM 校验 sec-ch-ua 时再对齐」，被追问才承认这不是给未来的预留，而是**当下已存在**的指纹自相矛盾（UA 说 120、sec-ch-ua 说随机 119~123）；随后「版本号散落两处、改 UA 忘改提示头」的风险又想靠注释提醒了事。说得出口的已知风险就是已知 bug 的候补名单，留着必然兑现，且兑现时排查成本远高于当场修

## 代码注释

基线：**不写 JSDoc、不留被注释掉的代码、不复述代码在做什么**。注释只用来记「为什么」。

- ❌ 写「这段代码在做什么」的注释（`// 遍历列表累加价格`、`// 设置默认预算`、`// 绑定点击事件`）
  后果：代码本身已经说清楚了，复述一遍是纯噪音；等代码改了、注释没跟着改，它就从噪音变成假信息

- ❌ 加 JSDoc / `@param` / `@returns` / 独立类型注解块
  后果：函数小、命名直白，又没有工具消费这些标签，写了只是让签名一改就多一处要同步、不同步就撒谎的地方。类型信息靠 TS 或调用点自明

- ❌ 把删掉的旧逻辑注释掉留在原地（`// const old = ...`、`// 老算法先留着`）
  后果：git 里全都有。留在代码里只会让接手者以为「哪天要恢复」，不敢动。删就删干净

- ❌ 用 `// TODO` / `// FIXME` / `// 以后再优化` 代替当场解决或立记录
  后果：见「工程习惯」——说得出口的已知风险就是已知 bug 的候补名单，留着必然兑现，兑现时排查成本远高于当场修

- ❌ 改动某段逻辑的行为，却不同步它旁边解释「原来 X → 会导致 Y → 所以改成 Z」的注释
  后果：这种决策链注释是接手者判断「这段能不能动」的唯一依据。代码变了、注释还留着旧结论，比没有注释更危险

- ❌ 数据表（跑分表、映射表、常量清单）不标来源 / 单位 / 口径，也不按类别分区
  后果：几百行裸数字缺了这些锚点，改一个值得先猜它是什么量纲、能不能信

## 技术栈与架构边界

- 根目录是 Electron 应用，`web/` 是独立网页版；依赖、启动命令、构建配置和运行环境各自留在对应目录。只改一端时不顺手改另一端，跨端协议改动才同时核对两边。
- 构建不用 webpack/rollup，可以采用一套配置管三端的方式，没必要拆两套
- TypeScript 5 `strict: true`，不关 strict、不甩 `any`——类型是唯一防线，关了等于裸奔。
- UI 不加新 UI 库/CSS-in-JS/class 组件——两套风格混用心智负担翻倍。
- 渲染进程状态管理不上 Redux/Zustand/MobX/Context 全局态——现有模式够用，多一层状态库只是多一层要背的抽象。
- 抓取场景不用 axios/undici/node-fetch/裸 Node `https`——`https` 不走系统代理会黑洞超时（见上「网络请求」）。
- 抓取解析不常驻 puppeteer/playwright——BGM/萌娘都是服务端渲染 HTML、没有 JS 挑战，上真浏览器只会更慢更吃内存。
- 文件监听不自写轮询——系统本身都有"文件变了主动通知你"的机制。
- 邮件用不接云邮件 SDK——本地 SMTP 够用，没必要多接一个账号依赖。
- 更新不自研协议——需要自己处理签名校验和增量包。
- 持久化不上 SQLite/IndexedDB/ORM——数据量小，JSON 读写够用。
- 没有测试框架，也不需要——不要自作主张加 Jest/Vitest，加了没人维护。
- 渲染进程不碰网络/文件/Node API，渲染进程只能请求 IPC 写死的功能（搜索、下载等），无法请求"读任意文件""执行任意命令"这种，防止黑客和恶意代码
- 不为不存在的需求（多租户、插件系统、国际化）预留扩展点——YAGNI，三行能写完不要抽成一层。

## DEVLOG 配图

图是给**项目经理和接手者**看的，不是给写代码的人看的。判据很硬：一个没读过这份代码的人，
只看图能不能说清「用户会经历什么」和「为什么非得这么做」。做不到就是图没画对。

基准样例：[`xifan-web-slow-source.svg`](./docs/devlog-assets/xifan-web-slow-source.svg)（链路图）、
[`xifan-web-player-states.svg`](./docs/devlog-assets/xifan-web-player-states.svg)（状态文案对照表）。

**内容**

- **结论先行**。标题下面一行先给结果（「96% 的番根本不碰服务器；只有慢源时才走这条路，用户等约 1 分钟」），
  别让读者读完全图才知道这事值不值。
- **分区块讲，每块回答一个问题**：瓶颈在哪 → 主链路怎么走 → 异常/边界怎么处理 → 哪里容易改错。
- **用户视角和底层视角分层画**。同一条时间轴，上半是「用户看到的」（含实际文案原文），
  下半是「背后在做的」。只画底层等于把图画给了自己。
- **数字必须是实测值**，并标出量纲和对比基准（「单连接 2.2Mbps < 码率 2.67Mbps」比「带宽不够」有用一百倍）。
  没测过就别写数字。
- **反直觉的设计必须解释**，而且要写成「为什么不那样做」：例如「为什么等待时不让用户先凑合看着」——
  因为凑合看会占满带宽把转码饿死，形成死锁。这类块是图里最值钱的部分，接手者最容易在这儿改错。
- **边界情况要画**：拖进度条到还没处理的位置会怎样、失败了退到哪里、名额满了怎么办。
  只画顺利路径的图，接手者一遇到异常就会自己发明一套行为。
- **同名概念要显式区分**。真实教训：「观看名额满」和「转码队列排队」都被写成「排队」，
  用户当场看混——两种「要等」必须在图里并排列出、写明互不相关。
- 状态/文案多的功能**另画一张对照表**：每条文案 → 出现在哪个位置 → 什么情况出现 → 用户当时能做什么。
- 结尾点明**常见情况**（「96% 的番一句提示都不会出现」），免得读者按提示出现频率误判功能价值。

**措辞**

- 用产品语言不用实现语言：「服务器开 12 条连接拉片」而不是「12 路并发 Range 请求」；
  「只是换个包装，不重新编码」而不是「remux `-c copy`」。
- 不写函数名、常量名、文件名。那些属于正文和代码注释。

**视觉**（沿用 `docs/devlog-assets/` 既有 SVG，不要另起一套）

- 外框 `#fbfafb` 底 + `#e2dcdf` 边，圆角 14；卡片圆角 9~10。
- 标题 19px/700 `#282529`，副标题 12.5px `#756f73`，块标题 14px/700，卡片标题 12.5px/700，正文 11px。
- 语义配色：正常/成功走绿（底 `#edf7f1` 边 `#7fad91` 字 `#316247`），
  问题/拦截走红（`#fdeef0` / `#d99aa5` / `#96434f`），
  机制说明走蓝（`#eef4f8` / `#91adc1` / `#36566d`），
  重点提示走黄（`#fff8e8` / `#d9bd85` / `#7a6234`），中性走灰（`#f4f2f5` / `#c3bcc6` / `#4a4450`）。
- 字体栈 `-apple-system,BlinkMacSystemFont,'PingFang SC','Microsoft YaHei',sans-serif`。
- 存进 `docs/devlog-assets/<专题>-<主题>.svg`，写完用 XML 解析器验一遍格式合法。

**交付**

- 图先给用户看、改到认可**再**写日志正文；日志按最终定稿的图来组织，不要图和文各说各的。

## 提交规范

- commit message 用 **Conventional Commits + 中文描述**：`<type>(<scope>): <描述>`
  - 允许的 type：`feat` `fix` `docs` `refactor` `chore` `style` `perf` `test` `build` `ci`
  - scope 可选，用模块名（`bgm` `ipc` `library` 等）
- 标题写用户/开发者能看懂的**现象或结果**，不堆底层术语（术语放正文）。
  例：`fix(bgm): 修复搜索动漫时报 net::ERR_INVALID_ARGUMENT`，不是 `netRequest 跳过 Host 头`。
- 一个功能、模块对应一个提交；不要把桌面应用、网页版或无关模块的改动混进同一提交。
- 正文按需写：简单改动只要标题；复杂/踩过坑的改动才写正文，正文只写关键原因/决策，不堆废话。
- **不加 AI 署名 trailer**（如 `Co-Authored-By: Claude ...`）—— 提交历史统一以开发者身份呈现。
- **只在用户明确要求时才 commit/push。** 「用户同意了某个修复方案」**不等于**授权 push —— 2026-07-17 真实事故：用户批准了修复顺序，我就把「push + 部署」当成了长期授权，一轮 push 了三次，用户一次都没让。后果不是洁癖问题：其中一次 `git add -A` 把用户自己删的 `docs/重构方案.md` 扫进了一个「改响应式 CSS」的提交里，**在用户有机会审之前就推上去了** —— 用户要的审查环节正是用来挡这个的。用户原话：「你没发现之前一直是我手动 push 嘛？」**默认流程：我写 → 用户审 → 用户 push → 我再上线。**
- **`git add -A` 之前先看清楚暂存了什么**（`git status` / `git diff --stat`）。用户可能正在同一个仓库里干活，`-A` 会把不属于你这次改动的增删一起卷进来，记到一个文不对题的提交上。
- **提交前先按 [DEVLOG.md](./DEVLOG.md) 的分类规则补一条日志**——这是交付前的必经步骤。先确定最终 commit 标题，日志标题写成 `YYYY-MM-DD <commit subject>`，`<commit subject>` 与实际提交标题必须逐字一致；提交标题变化时先同步日志。
  - 可归入专题的记录写 `docs/devlog/<年份>-<专题>.md`，暂时无法归类的单次记录写根 `DEVLOG.md`；新记录在上。
  - **修改后效果**：写清这次提交改变了什么最终行为；简单改动到这里即可，不逐文件复述 diff。同一未提交改动的多轮调整始终合并为一条最终记录。
  - **关键代码决策 / 状态流 / 数据流**：复杂或不显然的机制必须说明；优先用 `docs/devlog-assets/` 下的 SVG 或简短文本流程图，只有图说不清时才贴决定行为的代码。
  - **边界 / 风险 / 还存在的问题**：写尚未解决、仍依赖假设或接手者必须知道的限制。
  - DEVLOG 保留理解实现所需的关键决策；较长的失败尝试、方案权衡和阶段复盘写入对应 `docs/ideas/NNN-*.md`，再从日志链接。
